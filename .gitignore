# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
package-lock.json
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment files - NEVER COMMIT REAL CREDENTIALS!
.env
.env.backup
.env.local
.env.production
.env.*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Allow dummy test env to be committed (overrides global .env.* ignore)
!.env.test

.vercel

# Playwright MCP server artifacts (ephemeral browser session recordings,
# console logs, and page snapshots — not source code).
.playwright-mcp/
.codex-tmp/

# Temporary files left behind by FUSE/overlay filesystem operations.
.fuse_hidden*

.vercel
